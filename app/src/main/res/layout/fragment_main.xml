<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F0F1A">

    <!-- Поле ввода валюты -->
    <EditText
        android:id="@+id/currencyInput"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:hint="Введите валюту (usd, eur...)"
        android:textColor="#FFFFFF"
        android:textColorHint="#AAAAAA"
        android:background="@android:drawable/edit_text"
        android:padding="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/searchButton"
        app:layout_constraintHorizontalWeight="1"
        android:layout_margin="16dp"/>

    <!-- Кнопка поиска -->
    <Button
        android:id="@+id/searchButton"
        android:layout_width="60dp"
        android:layout_height="50dp"
        android:text="🔍"
        app:layout_constraintTop_toTopOf="@id/currencyInput"
        app:layout_constraintBottom_toBottomOf="@id/currencyInput"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="16dp"/>

    <!-- Название валюты -->
    <TextView
        android:id="@+id/currencyName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Название"
        android:textSize="24sp"
        android:textColor="#4FC3F7"
        app:layout_constraintTop_toBottomOf="@id/currencyInput"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"/>

    <!-- Цена -->
    <TextView
        android:id="@+id/currencyPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Курс: 0.00"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/currencyName"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
